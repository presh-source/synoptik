# Local Backend Configuration for LocalStack
# This file is used when deploying to LocalStack instead of AWS

# To use local backend, initialize with:
# terraform init -backend-config=backend-local.hcl

# Or disable backend entirely:
# terraform init -backend=false
